name: deploy-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  call-front-action:
    uses: ./.github/workflows/front-action.yml@v0.0.1
  call-back-action:
    uses: ./.github/workflows/back-action.yml@v0.0.1
  call-deploy-action:
    uses: ./.github/workflows/deploy-action.yml@v0.0.1     